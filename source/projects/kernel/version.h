#pragma once

// Single source of truth for the version reported to Max and to Jupyter.
// Keep in sync with the `version` field in pyproject.toml, and add an entry to
// CHANGELOG.md when bumping it.
#define MX_KERNEL_VERSION "0.2.0"
