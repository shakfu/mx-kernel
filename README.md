# mx-kernel

A Jupyter kernel embedded in a Max external

This is a working proof-of-concept and successfully allows two-way messaging between the `kernel` external and a jupyter-console instance running in the console.

Future iterations may develop further remote scripting features.